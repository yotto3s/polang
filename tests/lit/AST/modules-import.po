; RUN: %polang_compiler --dump-ast %s | %FileCheck %s

; Test import statement AST
; CHECK:      NBlock
; CHECK-NEXT: |-NModuleDeclaration 'Math' (add)
; CHECK:      `-NImportStatement import Math

module Math (add)
  let add(x: int, y: int): int = x + y
endmodule
import Math
