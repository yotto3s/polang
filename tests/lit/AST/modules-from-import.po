; RUN: %polang_compiler --dump-ast %s | %FileCheck %s

; Test from import statement AST
; CHECK:      NBlock
; CHECK-NEXT: |-NModuleDeclaration 'Math' (add, PI)
; CHECK:      `-NImportStatement from Math import add, PI

module Math (add, PI)
  let PI = 3.14
  let add(x: i64, y: i64): i64 = x + y
endmodule
from Math import add, PI
