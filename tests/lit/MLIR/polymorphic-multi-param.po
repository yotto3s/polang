; RUN: %polang_compiler --emit-mlir %s | %FileCheck %s

; Test polymorphic function with multiple untyped params in binary op.
; After monomorphization, should have specialized version for int.

; CHECK: {{.*}}polang.func @add$int_int(%{{.*}}: !polang.int, %{{.*}}: !polang.int) -> !polang.int{{.*}}

let add(x, y) = x + y
add(1, 2)
