; RUN: %polang_compiler --emit-mlir %s | %FileCheck %s

; Test polymorphic function specialization with double type.

; Specialized version for double
; CHECK-DAG: {{.*}}polang.func @identity$f64(%{{.*}}: !polang.float<64>) -> !polang.float<64>{{.*}}

; Original polymorphic function is preserved
; CHECK-DAG: {{.*}}polang.func @identity(%{{.*}}: !polang.typevar{{.*}}) -> !polang.typevar{{.*}} attributes {polang.polymorphic}{{.*}}

let identity(x) = x
identity(3.14)
