(* RUN: %polang_compiler --emit-mlir %s | %FileCheck %s
*)

(* Test polymorphic function specialization with double type. *)

(* Specialized version for double *)
(* CHECK-DAG: {{.*}}polang.func @identity$f64(%{{.*}}: f64) -> f64{{.*}}
*)

(* Original generic function is erased *)
(* CHECK-NOT: polang.generic_func
*)

identity(x) = x
identity(3.14)
