; RUN: %polang_compiler --emit-mlir %s | %FileCheck %s

; Polymorphic function, type inferred via monomorphization at call site

; Polymorphic template
; CHECK: {{.*}}polang.func @identity(%{{.*}}: !polang.typevar{{.*}}){{.*}}polang.polymorphic{{.*}}

; Monomorphized version for int
; CHECK: {{.*}}polang.func @identity$i64(%{{.*}}: !polang.integer<64, signed>) -> !polang.integer<64, signed>{{.*}}
let identity(x) = x in identity(42)
