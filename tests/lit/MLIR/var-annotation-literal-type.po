; RUN: %polang_compiler --emit-mlir %s | %FileCheck %s

; Test that type annotations propagate to literal MLIR types

; CHECK: polang.constant.integer 5 : !polang.integer<32, signed>
let x: i32 = 5

; CHECK: polang.constant.float {{.*}} : !polang.float<32>
let y: f32 = 3.0

y
