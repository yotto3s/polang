(* RUN: %polang_compiler --emit-mlir %s | %FileCheck %s
*)

(* Test polymorphic function that uses x twice (same type parameter). *)
(* When the same parameter is used multiple times, the type is consistent. *)

(* Specialized version for int *)
(* CHECK-DAG: {{.*}}polang.func @double_val$i64(%{{.*}}: !polang.integer<64, signed>) -> !polang.integer<64, signed>{{.*}}
*)

(* Original generic function is erased *)
(* CHECK-NOT: polang.generic_func
*)

double_val(x) = x + x
double_val(21)
