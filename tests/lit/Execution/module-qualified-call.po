(* RUN: %polang_repl %s | %FileCheck %s
*)
(* CHECK: 8 : i64
*)

module Math (add, mul)
  add : i64 * i64 -> i64
  add(x, y) = x + y
  mul : i64 * i64 -> i64
  mul(x, y) = x * y
endmodule
Math.add(2, Math.mul(2, 3))
