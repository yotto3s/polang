; RUN: %polang_repl %s | %FileCheck %s
; CHECK: 42 : int

; Test that polymorphic functions work correctly with multiple specializations.
; The identity function is called with both int and bool types.

let identity(x) = x
let a = identity(42)
let b = identity(true)
if b then a else 0
