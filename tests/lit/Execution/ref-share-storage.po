; RUN: %polang_repl %s | %FileCheck %s

; Test that `ref x` (without deref) creates an immutable reference
; that shares storage with x. When x is updated, dereferencing the
; immutable reference should see the new value.
; CHECK: 20 : i64
let x = mut 10
let y = ref x
x <- 20
*y
