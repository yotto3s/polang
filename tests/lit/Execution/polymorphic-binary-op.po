(* RUN: %polang_repl %s | %FileCheck %s
*)
(* CHECK: 42 : i64
*)

(* Test function that uses x twice with explicit type. *)
(* Note: Polymorphic version doesn't print result due to REPL type detection limitation. *)

double_val(x) = x + x
double_val(21)
