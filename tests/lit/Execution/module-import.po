; RUN: %polang_repl %s | %FileCheck %s
; CHECK: 5 : i64

module Math (add, sub)
  let add(x: i64, y: i64): i64 = x + y
  let sub(x: i64, y: i64): i64 = x - y
endmodule
from Math import add, sub
add(3, sub(4, 2))
