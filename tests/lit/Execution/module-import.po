(* RUN: %polang_repl %s | %FileCheck %s
*)
(* CHECK: 5 : i64
*)

module Math (add, sub)
  add : i64 * i64 -> i64
  add(x, y) = x + y
  sub : i64 * i64 -> i64
  sub(x, y) = x - y
endmodule
from Math import add, sub
add(3, sub(4, 2))
