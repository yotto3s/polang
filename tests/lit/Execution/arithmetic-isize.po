(* RUN: %polang_repl %s | %FileCheck %s
*)

(* Test arithmetic operations on isize values *)
(* CHECK: 42 : isize
*)
add_isize : isize * isize -> isize
add_isize(a, b) = a + b
add_isize(10 as isize, 32 as isize)
