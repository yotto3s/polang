(* RUN: %polang_repl %s | %FileCheck %s
*)
(* CHECK: 6 : i64
*)

module Math (add, mul)
  add : i64 * i64 -> i64
  add(x, y) = x + y
  mul : i64 * i64 -> i64
  mul(x, y) = x * y
endmodule
from Math import *
mul(2, add(1, 2))
