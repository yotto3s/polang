(* RUN: %polang_repl %s | %FileCheck %s
*)

(* Test arithmetic operations on usize values *)
(* CHECK: 42 : usize
*)
add_usize : usize * usize -> usize
add_usize(a, b) = a + b
add_usize(10 as usize, 32 as usize)
