; RUN: %polang_compiler %s | %FileCheck %s

; Test recursive function generates correct call pattern
; CHECK: {{.*}}@factorial{{.*}}
; CHECK: {{.*}}call{{.*}}@factorial{{.*}}
let factorial(n: i64): i64 = if n <= 1 then 1 else n * factorial(n - 1)
