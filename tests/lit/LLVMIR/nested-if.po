; RUN: %polang_compiler %s | %FileCheck %s

; Test nested if expression generates correct branch structure
; CHECK: {{.*}}icmp{{.*}}
; CHECK: {{.*}}br {{.*}}
; CHECK: {{.*}}icmp{{.*}}
; CHECK: {{.*}}br {{.*}}
let f(x: int): int = if x > 0 then if x > 10 then 1 else 2 else 3
