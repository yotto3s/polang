; RUN: %polang_compiler %s | %FileCheck %s

; Test let-expression lowering
; Immutable variables are optimized to SSA values, enabling constant folding
; CHECK: define i64 @__polang_entry() {
; CHECK: {{.*}}ret i64 2
; CHECK: }
let x = 1 in x + 1
