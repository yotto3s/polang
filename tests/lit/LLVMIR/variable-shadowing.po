; RUN: %polang_compiler %s | %FileCheck %s

; Test variable shadowing in nested let expressions
; The inner x shadows the outer x; only one alloca needed since outer is unused
; CHECK: {{.*}}alloca{{.*}}
; CHECK: {{.*}}store{{.*}}
; CHECK: {{.*}}load{{.*}}
let x = 1 in let x = 2 in x
