(* RUN: %not %polang_repl %s 2>&1 | %FileCheck %s
*)

(* F1: RemOpLowering is missing the division-by-zero guard that DivOpLowering has.
   Modulo by zero (a % 0) should produce the same runtime error as division
   by zero (a / 0), but instead it causes undefined behavior / crash.
*)
x = 10 % 0
(* CHECK: Runtime error: integer division by zero at line 1, column 5
*)
