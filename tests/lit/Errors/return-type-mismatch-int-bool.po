(* RUN: %not %polang_compiler %s 2>&1 | %FileCheck %s
*)

(* Test that integer literal is NOT compatible with bool return type *)
(* CHECK: {{.*}}declared to return bool but body has type i64{{.*}}
*)
h : () -> bool
h() = 3
