(* RUN: %not %polang_compiler %s 2>&1 | %FileCheck %s
*)

(* Test let binding with type annotation literal range overflow *)
(* CHECK: {{.*}}literal 1000 does not fit in type i8 (range -128 to 127){{.*}}
*)
let x : i8 = 1000 in x
