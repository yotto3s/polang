(* RUN: %not %polang_compiler %s 2>&1 | %FileCheck %s
*)

(* CHECK: Type error: 'helper' is not exported from module 'Private'
*)

module Private
  helper : i64 -> i64
  helper(x) = x * 2
endmodule
Private.helper(5)
