(* RUN: %polang_compiler %s 2>&1 | %FileCheck %s
*)

(* Test that declaring a type variable in forall but not using it produces a warning. *)
(* CHECK: {{.*}}unused type variable{{.*}}'b{{.*}}
*)
unused : forall 'a, 'b. 'a -> 'a
unused(x) = x
unused(42)
