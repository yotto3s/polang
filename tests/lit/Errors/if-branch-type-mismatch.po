; RUN: %not %polang_compiler %s 2>&1 | %FileCheck %s

; Test if branch type mismatch (then and else have different types)
; CHECK: {{.*}}If branches have different types: i64 and f64{{.*}}
if true then 1 else 2.0
