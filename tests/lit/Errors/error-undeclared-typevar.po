(* RUN: %not %polang_compiler %s 2>&1 | %FileCheck %s
*)

(* Test that type variables used but not declared in forall are caught. *)
(* CHECK: {{.*}}undeclared type variable{{.*}}
*)
qux : forall 'a. 'a -> 'b
qux(x) = x
